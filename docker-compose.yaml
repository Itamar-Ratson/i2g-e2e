services:
  i2gw-test:
    build: .
    image: i2gw-test:latest
    privileged: true # Required for Docker-in-Docker
    stdin_open: true
    tty: true
    volumes:
      - ./tests:/workspace/tests # Mount your test files
