services:
  i2gw-test:
    build: .
    image: i2gw-test:latest
    privileged: true # Required for Docker-in-Docker
    stdin_open: true
    tty: true
    volumes:
      # Mount tests from your repo (for development)
      - ./tests:/workspace/tests
